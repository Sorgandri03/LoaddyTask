services:
  notification-service:
    container_name: notification-container
    build:
      context: ./notification
    ports:
      - "3002:3002"
    networks:
      - my-network

networks:
  my-network: